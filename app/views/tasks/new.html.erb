<div class="breadcrumbs breadcrumb_style">
	<span class="fa fa-home"></span> / Tasks / <span class="active">New</span>
</div>
<div>
	<%= form_with model: @task do |form|%>
		<%= render "form", form: form%>
	<%end%>
</div>